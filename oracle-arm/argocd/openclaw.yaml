apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openclaw
  namespace: argo-cd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  sources:
    - repoURL: https://serhanekicii.github.io/openclaw-helm
      chart: openclaw
      targetRevision: 1.5.27
      helm:
        valuesObject:
          app-template:
            configMode: overwrite
            controllers:
              main:
                initContainers:
                  init-skills:
                    command:
                      - sh
                      - -c
                      - |
                        set -e
                        BIN_DIR=/home/node/.openclaw/bin
                        mkdir -p "$BIN_DIR"
                        if [ ! -x "$BIN_DIR/gh" ]; then
                          GH_VERSION=2.92.0
                          curl -fsSL "https://github.com/cli/cli/releases/download/v${GH_VERSION}/gh_${GH_VERSION}_linux_arm64.tar.gz" | tar xz -C /tmp
                          cp "/tmp/gh_${GH_VERSION}_linux_arm64/bin/gh" "$BIN_DIR/gh"
                        fi
                        if [ ! -x "$BIN_DIR/kubectl" ]; then
                          curl -fsSL "https://dl.k8s.io/release/$(curl -fsSL https://dl.k8s.io/release/stable.txt)/bin/linux/arm64/kubectl" -o "$BIN_DIR/kubectl"
                        fi
                        chmod +x "$BIN_DIR/gh" "$BIN_DIR/kubectl"
                        echo "Tools installed:" && ls -la "$BIN_DIR"
                        exec /scripts/init-skills.sh
                containers:
                  main:
                    env:
                      PATH: "/home/node/.openclaw/bin:/root/.bun/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
                    envFrom:
                      - secretRef:
                          name: openclaw-secrets
            configMaps:
              config:
                data:
                  openclaw.json: |
                    {
                      "gateway": {
                        "port": 18789,
                        "mode": "local",
                        "trustedProxies": ["10.0.0.0/8"],
                        "controlUi": {
                          "dangerouslyAllowHostHeaderOriginFallback": true
                        }
                      },
                      "browser": {
                        "enabled": true,
                        "defaultProfile": "default",
                        "profiles": {
                          "default": {
                            "cdpUrl": "http://localhost:9222",
                            "color": "#4285F4"
                          }
                        }
                      },
                      "agents": {
                        "defaults": {
                          "workspace": "/home/node/.openclaw/workspace",
                          "model": "google/gemini-3.1-pro-preview",
                          "userTimezone": "UTC",
                          "timeoutSeconds": 600,
                          "maxConcurrent": 2
                        }
                      },
                      "session": {
                        "scope": "per-sender",
                        "store": "/home/node/.openclaw/sessions",
                        "reset": {
                          "mode": "idle",
                          "idleMinutes": 60
                        }
                      },
                      "logging": {
                        "level": "info",
                        "consoleLevel": "info",
                        "consoleStyle": "compact",
                        "redactSensitive": "tools"
                      },
                      "tools": {
                        "profile": "full",
                        "web": {
                          "search": { "enabled": true },
                          "fetch": { "enabled": true }
                        }
                      },
                      "channels": {
                        "telegram": {
                          "enabled": true,
                          "botToken": "${TELEGRAM_BOT_TOKEN}",
                          "dmPolicy": "pairing"
                        }
                      }
                    }
            ingress:
              main:
                enabled: true
                className: tailscale
                defaultBackend:
                  service:
                    name: openclaw
                    port:
                      number: 18789
                tls:
                  - hosts:
                      - openclaw
    - repoURL: https://github.com/belgio99/homelab.git
      targetRevision: main
      path: oracle-arm/openclaw
  destination:
    server: https://kubernetes.default.svc
    namespace: openclaw
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
